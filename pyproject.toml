[project]
name = "mini-llm-infra"
version = "0.1.0"
requires-python = ">=3.12,<3.13"
dependencies = [
    "huggingface-hub>=1.28.0",
    "numpy>=2.5.2",
    "triton>=3.7.1",
]

[build-system]
requires = ["uv_build>=0.12.5,<0.13"]
build-backend = "uv_build"
